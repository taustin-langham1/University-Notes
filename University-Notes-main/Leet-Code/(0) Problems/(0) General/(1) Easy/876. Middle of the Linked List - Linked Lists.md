


### Attempt (failed) (learned a lot though!):

```
  

public static ListNode middleNode(ListNode head){
    ListNode current = head;
    ArrayList<Integer> nums = new ArrayList<>();
    int cntr = 0;

    while(current != null){
        nums.add(current.val);
        current = current.next;
        cntr++;
    }

    if ((cntr /= 2) % 2 != 0){
           cntr = (int) Math.ceil(cntr / 2);
    }
    else{
        cntr /= 2;
    }

  
    current = head;
    while(current != null){
        if (current.val == cntr){
            head = current;
        }
        current = current.next;
    }
    return head;
}
```


### Answer:

-- I think I panicked at the thought of the logistics of converting an array (array list at that) into a linked list (vica versa). I didn't know (now do) that Array List can be of own type, not just some set list. 

```

public ListNode middleNode(ListNode head){
	ArrayList <ListNode> array = new ArrayList<>();
	
	int length = 0;
	
	while(head != null){
		array.add(head);
		head = head.next;
		length++
	}
	
	return array.get(length / 2);
	
	// will this ^ just pick the ceiling then?
}

```